{{- if eq .Values.postgres.enabled true }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-postgres
  labels:
    {{- include "european-reference-data-registry.selectorLabels.postgres" . | nindent 4 }}
spec:
  selector:
    {{- include "european-reference-data-registry.selectorLabels.postgres" . | nindent 4 }}
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
{{- end }}
